import './Navbar.css';

function Navbar() {
  return (
    <header id="navbar">
      <nav>
        <a href="#home">Home</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>
  );
}

export default Navbar;
